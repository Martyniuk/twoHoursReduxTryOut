// Core
import React, { Component } from 'react';

// CSS
import Styles from './styles.css';

class Navigation extends Component {
    render () {
        return (
            <div className = { Styles.header }>{'Router could be here'}</div>
        );
    }
}

export default Navigation;
